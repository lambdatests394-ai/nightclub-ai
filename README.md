# NIGHT CLUB AI v1.0

Phase 1 scaffold only. The architecture is documented in `docs/` and no domain modules, migrations, Meta integrations, AI integrations, or n8n workflows are implemented yet.

Run locally after installing dependencies:

```powershell
.\.venv\Scripts\python.exe -m uvicorn backend.app.main:app --reload
```
